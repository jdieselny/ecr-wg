<?xml version="1.0" encoding="UTF-8"?>
<!--
  Session lifecycle diagram — STUB placeholder.
  This file is a reserved path. The real diagram will be authored in a future
  session covering: POST → Load Continuum → Work → Commit this → Handoff to
  Mrs. Code → Verify. Includes the three-actor model (Justin, SEV, Mrs. Code)
  and the sanitization audit point.

  When this is built, match the visual language of system_overview.svg:
  black title bar, DC_RED accent, DC_BLUE for SEV, DC_RED for Mrs. Code.
-->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 200" font-family="system-ui, sans-serif">
  <rect x="0" y="0" width="800" height="200" fill="#FAFBFC"/>
  <rect x="0" y="0" width="800" height="40" fill="#1A1A1A"/>
  <text x="400" y="27" font-size="14" font-weight="700" fill="#FFFFFF" text-anchor="middle">Session Lifecycle Diagram · STUB · scheduled for future session</text>
  <text x="400" y="110" font-size="13" fill="#666666" text-anchor="middle">This file is a reserved path. The real diagram will show:</text>
  <text x="400" y="135" font-size="13" fill="#1A1A1A" text-anchor="middle" font-weight="600">POST → Load Continuum → Work → Commit this → Handoff → Verify</text>
  <text x="400" y="170" font-size="11" fill="#999999" text-anchor="middle">See /docs/01_architecture/diagrams/system_overview.svg for the current architectural visual.</text>
</svg>
