# Local private keys (gitignored)

This directory holds **operator-local** `*_private_key.pem` files when you run enrollment tooling. Nothing here is tracked.

Generate with:

```bash
python scripts/enroll-ed25519.py
```

See `../README.md` and `../../keys/README.md`.